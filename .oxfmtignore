node_modules
.next
out
public
docs/governance/manifest.json
docs/governance/MANIFEST.yml
# PGS-managed rule snapshots. oxfmt pads markdown tables, which changes the
# content hash and trips `pro-gov portfolio assets-check` with hash-drift.
# These files are owned upstream — never reformat them locally.
docs/policy/shared-rules
# PGS starter governance docs are distributed content, not ours to restyle.
docs/governance
# Local QA scripts, not part of the shipped bundle.
tools
